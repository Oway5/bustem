SCRAPER_API_KEY=4558fb24345f6ac0aa999ef5d14f5ea9

# Orchestration knobs (all optional, falls back to safe defaults)
# Total external requests (scrape + image) allowed per job.
BUSTEM_BUDGET_CAP=120
# Max concurrent ScraperAPI calls.
BUSTEM_SCRAPER_CONCURRENCY=5
# Max concurrent image downloads during the image-scoring phase.
BUSTEM_IMAGE_CONCURRENCY=6
# Cap on how many coarse-ranked listings get image-scored.
BUSTEM_MAX_SHORTLIST=30
